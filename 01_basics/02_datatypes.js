"use strict"; //treat all JS code as newer version
// alert(3+#) we are using nodejs not browser, in browser it will run as it is inside the document
console.log(3+3);

console.log("Sri");
let name="Sri"
let age=21
let isLoggedIn=false
let state=null

// number -> 2 to power 53
// bigint 
// string -> ""
// boolean -> true/false
// null -> standalone value
// undefined -> variable defined but not value is assigned
// symbol -> unique


// object

console.log(typeof null); //object
console.log(typeof undefined); //undefined
